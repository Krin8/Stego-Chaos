output_root: '../'
pytorch_data_dir: '/content/drive/MyDrive/STEGO/src/pytorch_data_dir'
experiment_name: "chaos"
res: 320
batch_size: 8
num_workers: 0
run_picie: True
run_crf: True
run_prediction: True
dark_mode: True
use_ddp: False

model_paths:
  - "/content/drive/MyDrive/STEGO/checkpoints/chaos/mIoU=46.8485.ckpt"

hydra:
  run:
    dir: "."
  output_subdir: ~
  #job_logging: "disabled"
  #hydra_logging: "disabled"
