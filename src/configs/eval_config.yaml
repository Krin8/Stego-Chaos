output_root: '../'
pytorch_data_dir: '/content/drive/MyDrive/STEGO/src/pytorch_data_dir'
experiment_name: "chaos"
res: 320
batch_size: 16
num_workers: 0
run_picie: True
run_crf: True
run_prediction: True
dark_mode: True
use_ddp: False

model_paths:
  - "/content/drive/MyDrive/STEGO/checkpoints/chaos/mIoU=46.8485.ckpt"
model_sha256s:
  # Set each entry to the trusted output of: sha256sum <model_path>
  - null

hydra:
  run:
    dir: "."
  output_subdir: ~
  #job_logging: "disabled"
  #hydra_logging: "disabled"
